
(function () {
var scripts = [{"deps":{"./assets/Script/GameTools":17,"./assets/Script/GameUiTools":2,"./assets/Script/Home":4,"./assets/Script/Number":6,"./assets/Script/eq4096":13,"./assets/Script/eqconf":7,"./assets/Script/main":8,"./assets/Script/DeskSprite":10,"./assets/Script/panel/HowGame":1,"./assets/Script/panel/JiasuPanel":9,"./assets/Script/panel/Jptj":12,"./assets/Script/panel/LixianPanel":11,"./assets/Script/panel/MiniProgram":14,"./assets/Script/panel/MoneyPanel":15,"./assets/Script/panel/NewPersion":16,"./assets/Script/panel/RankItem":18,"./assets/Script/panel/RankingListView":19,"./assets/Script/panel/SelfApp":22,"./assets/Script/panel/ShopItemList":20,"./assets/Script/panel/ShopPanel":21,"./assets/Script/panel/ShopTopLevel":23,"./assets/Script/panel/ShowMessage":29,"./assets/Script/panel/Sign":28,"./assets/Script/panel/TurnTable":25,"./assets/Script/panel/CblPanel":26,"./assets/Script/sdk/EyouClientKs":5,"./assets/migration/use_reversed_rotateTo":3,"./assets/migration/use_v2.1-2.2.1_cc.Toggle_event":24,"./assets/migration/use_reversed_rotateBy":30,"./assets/Script/GameConfig":27},"path":"preview-scripts/__qc_index__.js"},{"deps":{"GameTools":17},"path":"preview-scripts/assets/Script/panel/HowGame.js"},{"deps":{},"path":"preview-scripts/assets/Script/GameUiTools.js"},{"deps":{},"path":"preview-scripts/assets/migration/use_reversed_rotateTo.js"},{"deps":{"GameTools":17,"GameUiTools":2,"GameConfig":27},"path":"preview-scripts/assets/Script/Home.js"},{"deps":{},"path":"preview-scripts/assets/Script/sdk/EyouClientKs.js"},{"deps":{},"path":"preview-scripts/assets/Script/Number.js"},{"deps":{},"path":"preview-scripts/assets/Script/eqconf.js"},{"deps":{"GameTools":17,"GameUiTools":2,"GameConfig":27},"path":"preview-scripts/assets/Script/main.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/JiasuPanel.js"},{"deps":{"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/DeskSprite.js"},{"deps":{"GameConfig":27,"GameUiTools":2,"GameTools":17},"path":"preview-scripts/assets/Script/panel/LixianPanel.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/Jptj.js"},{"deps":{"./eqconf.js":7},"path":"preview-scripts/assets/Script/eq4096.js"},{"deps":{"GameTools":17},"path":"preview-scripts/assets/Script/panel/MiniProgram.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/MoneyPanel.js"},{"deps":{"GameConfig":27,"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/panel/NewPersion.js"},{"deps":{"GameConfig":27,"eq4096":13},"path":"preview-scripts/assets/Script/GameTools.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/RankItem.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/RankingListView.js"},{"deps":{"GameConfig":27,"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/panel/ShopItemList.js"},{"deps":{"GameConfig":27,"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/panel/ShopPanel.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/SelfApp.js"},{"deps":{"GameConfig":27,"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/panel/ShopTopLevel.js"},{"deps":{},"path":"preview-scripts/assets/migration/use_v2.1-2.2.1_cc.Toggle_event.js"},{"deps":{"GameConfig":27,"GameTools":17},"path":"preview-scripts/assets/Script/panel/TurnTable.js"},{"deps":{"GameConfig":27},"path":"preview-scripts/assets/Script/panel/CblPanel.js"},{"deps":{},"path":"preview-scripts/assets/Script/GameConfig.js"},{"deps":{"GameConfig":27,"GameTools":17,"GameUiTools":2},"path":"preview-scripts/assets/Script/panel/Sign.js"},{"deps":{},"path":"preview-scripts/assets/Script/panel/ShowMessage.js"},{"deps":{},"path":"preview-scripts/assets/migration/use_reversed_rotateBy.js"}];
var entries = ["preview-scripts/__qc_index__.js"];
var bundleScript = 'preview-scripts/__qc_bundle__.js';

/**
 * Notice: This file can not use ES6 (for IE 11)
 */
var modules = {};
var name2path = {};

// Will generated by module.js plugin
// var scripts = ${scripts};
// var entries = ${entries};
// var bundleScript = ${bundleScript};

if (typeof global === 'undefined') {
    window.global = window;
}

var isJSB = typeof jsb !== 'undefined';

function getXMLHttpRequest () {
    return window.XMLHttpRequest ? new window.XMLHttpRequest() : new ActiveXObject('MSXML2.XMLHTTP');
}

function downloadText(url, callback) {
    if (isJSB) {
        var result = jsb.fileUtils.getStringFromFile(url);
        callback(null, result);
        return;
    }

    var xhr = getXMLHttpRequest(),
        errInfo = 'Load text file failed: ' + url;
    xhr.open('GET', url, true);
    if (xhr.overrideMimeType) xhr.overrideMimeType('text\/plain; charset=utf-8');
    xhr.onload = function () {
        if (xhr.readyState === 4) {
            if (xhr.status === 200 || xhr.status === 0) {
                callback(null, xhr.responseText);
            }
            else {
                callback({status:xhr.status, errorMessage:errInfo + ', status: ' + xhr.status});
            }
        }
        else {
            callback({status:xhr.status, errorMessage:errInfo + '(wrong readyState)'});
        }
    };
    xhr.onerror = function(){
        callback({status:xhr.status, errorMessage:errInfo + '(error)'});
    };
    xhr.ontimeout = function(){
        callback({status:xhr.status, errorMessage:errInfo + '(time out)'});
    };
    xhr.send(null);
};

function loadScript (src, cb) {
    if (typeof require !== 'undefined') {
        require(src);
        return cb();
    }

    // var timer = 'load ' + src;
    // console.time(timer);

    var scriptElement = document.createElement('script');

    function done() {
        // console.timeEnd(timer);
        // deallocation immediate whatever
        scriptElement.remove();
    }

    scriptElement.onload = function () {
        done();
        cb();
    };
    scriptElement.onerror = function () {
        done();
        var error = 'Failed to load ' + src;
        console.error(error);
        cb(new Error(error));
    };
    scriptElement.setAttribute('type','text/javascript');
    scriptElement.setAttribute('charset', 'utf-8');
    scriptElement.setAttribute('src', src);

    document.head.appendChild(scriptElement);
}

function loadScripts (srcs, cb) {
    var n = srcs.length;

    srcs.forEach(function (src) {
        loadScript(src, function () {
            n--;
            if (n === 0) {
                cb();
            }
        });
    })
}

function formatPath (path) {
    let destPath = window.__quick_compile_project__.destPath;
    if (destPath) {
        let prefix = 'preview-scripts';
        if (destPath[destPath.length - 1] === '/') {
            prefix += '/';
        }
        path = path.replace(prefix, destPath);
    }
    return path;
}

window.__quick_compile_project__ = {
    destPath: '',

    registerModule: function (path, module) {
        path = formatPath(path);
        modules[path].module = module;
    },

    registerModuleFunc: function (path, func) {
        path = formatPath(path);
        modules[path].func = func;

        var sections = path.split('/');
        var name = sections[sections.length - 1];
        name = name.replace(/\.(?:js|ts|json)$/i, '');
        name2path[name] = path;
    },

    require: function (request, path) {
        var m, requestScript;

        path = formatPath(path);
        if (path) {
            m = modules[path];
            if (!m) {
                console.warn('Can not find module for path : ' + path);
                return null;
            }
        }

        if (m) {
            let depIndex = m.deps[request];
            // dependence script was excluded
            if (depIndex === -1) {
                return null;
            }
            else {
                requestScript = scripts[ m.deps[request] ];
            }
        }
        
        let requestPath = '';
        if (!requestScript) {
            // search from name2path when request is a dynamic module name
            if (/^[\w- .]*$/.test(request)) {
                requestPath = name2path[request];
            }

            if (!requestPath) {
                if (CC_JSB) {
                    return require(request);
                }
                else {
                    console.warn('Can not find deps [' + request + '] for path : ' + path);
                    return null;
                }
            }
        }
        else {
            requestPath = formatPath(requestScript.path);
        }

        let requestModule = modules[requestPath];
        if (!requestModule) {
            console.warn('Can not find request module for path : ' + requestPath);
            return null;
        }

        if (!requestModule.module && requestModule.func) {
            requestModule.func();
        }

        if (!requestModule.module) {
            console.warn('Can not find requestModule.module for path : ' + path);
            return null;
        }

        return requestModule.module.exports;
    },

    run: function () {
        entries.forEach(function (entry) {
            entry = formatPath(entry);
            var module = modules[entry];
            if (!module.module) {
                module.func();
            }
        });
    },

    load: function (cb) {
        var self = this;

        var srcs = scripts.map(function (script) {
            var path = formatPath(script.path);
            modules[path] = script;

            if (script.mtime) {
                path += ("?mtime=" + script.mtime);
            }
            return path;
        });

        console.time && console.time('load __quick_compile_project__');
        // jsb can not analysis sourcemap, so keep separate files.
        if (bundleScript && !isJSB) {
            downloadText(formatPath(bundleScript), function (err, bundleSource) {
                console.timeEnd && console.timeEnd('load __quick_compile_project__');
                if (err) {
                    console.error(err);
                    return;
                }

                let evalTime = 'eval __quick_compile_project__ : ' + srcs.length + ' files';
                console.time && console.time(evalTime);
                var sources = bundleSource.split('\n//------QC-SOURCE-SPLIT------\n');
                for (var i = 0; i < sources.length; i++) {
                    if (sources[i]) {
                        window.eval(sources[i]);
                        // not sure why new Function cannot set breakpoints precisely
                        // new Function(sources[i])()
                    }
                }
                self.run();
                console.timeEnd && console.timeEnd(evalTime);
                cb();
            })
        }
        else {
            loadScripts(srcs, function () {
                self.run();
                console.timeEnd && console.timeEnd('load __quick_compile_project__');
                cb();
            });
        }
    }
};

// Polyfill for IE 11
if (!('remove' in Element.prototype)) {
    Element.prototype.remove = function () {
        if (this.parentNode) {
            this.parentNode.removeChild(this);
        }
    };
}
})();
    